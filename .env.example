# Google OAuth 2.0 Client ID for Google Identity Services
# Obtain from https://console.cloud.google.com/apis/credentials
# Used by AuthService and set at build time via Angular environments
NG_APP_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
